<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.dao.DepartDao">
  <!-- 게시판 리스트 -->
  <select id="selectAll" resultType="com.java.dto.DepartDto">
    select * from departments
    <!--select department_id, department_name, ifnull(parent_id, 0), ifnull(manager_id, 0), create_date, update_date from departments -->
  </select>
 </mapper>
